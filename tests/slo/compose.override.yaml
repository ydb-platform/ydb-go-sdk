services:
  workload-current:
    build:
      context: ../..
      dockerfile: tests/slo/Dockerfile
      args:
        SRC_PATH: ${SRC_PATH:-native/table}
      platforms:
        - linux/amd64
    image: ${WORKLOAD_CURRENT_IMAGE:-ydb-app-current}
