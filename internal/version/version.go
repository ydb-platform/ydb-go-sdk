package version

const (
	Major = "3"
	Minor = "127"
	Patch = "7"

	Package = "ydb-go-sdk"
)

const (
	Version     = Major + "." + Minor + "." + Patch + "-hotfix.3"
	FullVersion = Package + "/" + Version
)
