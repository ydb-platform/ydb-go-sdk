package version

const (
	Major = "3"
	Minor = "127"
	Patch = "7"

	Package = "ydb-go-sdk"
)

const (
	Version     = Major + "." + Minor + "." + Patch + "-hotfix.4"
	FullVersion = Package + "/" + Version
)
